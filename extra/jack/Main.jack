class Main {
    function void main() {
        
        var Array a;
        var int length;
        var int i, sum;

        let length = Keyboard.readInt("Mi canaima?: ");
        let a = Array.new(length);
        let i = 0;

        while (i < length) {
            // code
            let a[i] = Keyboard.readInt("Ingrese coñio valor: ");
            let i = i + 1;
        }

        return;
    }
}